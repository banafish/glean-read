## 新增需求

### 需求:大模型聚合碎片生成大纲
系统必须能够根据选定的标签集合与关联碎片，交由 LLM 进行提纯总结，输出可回溯至明细碎片的树状系统大纲。

#### 场景:触发大纲合成
- **当** 用户在 PC 端控制台选中 `#Android性能优化` 标签下的 5 件散落碎片，并点击“AI总纲合成”功能按钮时
- **那么** 系统在后台拼接含有这些碎片的 Prompt 交由 LLM 运算，返回包含引用锚点的结构化 Markdown 纲要文本，并在用户的知识森林中创建一个包含这 5 条叶节点的树干节点
