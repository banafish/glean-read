## 修改需求

### 需求: 纯粹的知识树摘录挂载及节点指定支持
改造现有的挂载相关接口逻辑，具体涉及：
1. **显式支持指定挂载节点**: 挂载动作能够明确接收一个指定的 `targetNodeId`。如果不为空，则将所选中的摘录集合作为该目标节点的下级内容（或通过该关联表绑死）进行挂载。
2. **剥离自动化摘要**: 将之前“挂载时自动后台唤起 AI 并生成包含整棵树或该节点总结摘要的逻辑”进行强制剥离。新的挂载接口职责单一化：仅处理实体间的联系（摘录 <-> 知识树），不再产生生成性质的模型请求开销和时延。
