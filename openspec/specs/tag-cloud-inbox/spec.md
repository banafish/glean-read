# 规范：tag-cloud-inbox

## 目的
处理和缓冲所有来自前端未被梳理的原始知识摘录，并通过标签词频统计展现当前开发者的主要兴趣聚热点。

## 需求

### 需求:自动汇入待处理区与标签热度追踪
系统必须将所有移动端新采集的摘录首先置入 Inbox 收件箱，并实时累加该摘录所附标签的热度统计。

#### 场景:接收新摘录触发热度云更新
- **当** 一条带有 `#架构` 和 `#后端` 标签的文本摘录成功保存到服务器时
- **那么** 系统将该条记录显示在 Inbox 分页列表中，并在标签云的数据集里将 `#架构` 和 `#后端` 的热力权重分别累加 1
